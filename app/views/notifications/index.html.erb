<% provide(:title, 'Thông báo') %>
<%= render 'shared/pages/navigation' %>
<div class="wrapper wrapper-content animated fadeInRight">
  <div class="row">
    <div class="col-lg-12">
      <div class="ibox float-e-margins">
        <div class="ibox-content">

          <table class="table table-striped table-bordered table-hover dataTables-example">
            <thead>
            <tr>
              <th>Tiêu đề</th>
              <th>Nội dung</th>
              <th>Tệp</th>
              <th>Ngày tạo</th>
            </tr>
            </thead>
            <tbody>
            <%= render 'notifications/notifications', objects: @page_notifications %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>
<% if @page_notifications.count > 10 %>
  <%= render 'shared/pages/pagy_navigation' %>
<% end %>
<%= render 'shared/pages/footer' %>

