<div class="row">
  <div class="col-lg-12">
    <div class="ibox float-e-margins">
      <div class="ibox-title">
        <h5>NHÂN VIÊN</h5>
        <div class="ibox-tools">
          <%= link_to "Thêm mới", new_admin_user_path, class:"btn btn-primary btn-xs" %>
        </div>
      </div>
    </div>
  </div>
</div>
<div class="row">
  <% @users.each do |user| %>
    <div class="col-lg-3">
      <div class="contact-box center-version">
        <%= link_to edit_admin_user_path(user) do %>
          <%= image_tag(user.avatar.url, class: "img-circle") %>
          <h3 class="m-b-xs"><strong><%= user.full_name %></strong></h3>
          <div class="font-bold"><%= user.group_name %></div>
          <address class="m-t-md">
            <strong><%= user.company_name %></strong><br>
            <%= user.address %><br>
            <span>Email:</span> <%= user.email %>
          </address>
        <% end %>
        <div class="contact-box-footer">
          <div class="m-t-xs btn-group">
            <a class="btn btn-xs btn-white"><i class="fa fa-fax" aria-hidden="true"></i> Fax: <%= user.fax %></a>
            <a class="btn btn-xs btn-white"><i class="fa fa-envelope"></i> SĐT: <%= user.cell_phone %></a>
          </div>
        </div>
      </div>
    </div>
  <% end %>
</div>