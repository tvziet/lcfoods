<div class="row">
  <div class="col-lg-12">
    <div class="ibox float-e-margins">
      <div class="ibox-title">
        <h5>BĂNG RÔN</h5>
        <div class="ibox-tools">
          <%= link_to "Thêm mới", them_moi_bang_ron_path, class:"btn btn-primary btn-xs" %>
        </div>
      </div>
      <div class="ibox-content">
        <table class="table table-striped table-bordered table-hover dataTables-example">
          <thead>
          <tr>
            <th>Tệp</th>
            <th>Trạng thái</th>
            <th>Tác vụ</th>
          </tr>
          </thead>
          <tbody>
          <% @carousels.each do |carousel| %>
            <tr class="gradeA">
              <td>
                <div class="file-box">
                  <div class="file">
                    <%= image_tag(carousel.image, class:"img-responsive") %>
                  </div>
                </div>
              </td>
              <td>
                <% if carousel.active? %>
                  <span class="label label-danger"><%= carousel.status_i18n %></span>
                <% else %>
                  <span class="label label-default"><%= carousel.status_i18n %></span>
                <% end %>
              </td>
              <td>
                <%= link_to cap_nhat_bang_ron_path(carousel), class:"btn btn-white btn-sm" do %>
                  <i class="fa fa-pencil"></i> Cập nhật
                <% end %>
                <%= link_to xoa_bang_ron_path(carousel), method: :delete, data: { confirm: "Bạn có chắc chắc muốn xoá băng rôn này?" }, class:"btn btn-white btn-sm" do %>
                  <i class="fa fa-trash"></i> Xoá
                <% end %>
              </td>
            </tr>
          <% end %>
          </tbody>
        </table>
        <% if @carousels.count > 10 %>
          <%= render 'shared/pages/pagy_navigation' %>
        <% end %>
      </div>
    </div>
  </div>
</div>