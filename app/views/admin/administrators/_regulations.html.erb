<div class="row">
  <div class="col-lg-12">
    <div class="ibox float-e-margins">
      <div class="ibox-title">
        <h5>QUY ĐỊNH GẦN ĐÂY</h5>
      </div>
      <div class="ibox-content">
        <table class="table table-striped table-bordered table-hover dataTables-example">
          <thead>
          <tr>
            <th>Tiêu đề</th>
            <th>Nội dung</th>
            <th>Trạng thái</th>
            <th>Ngày tạo</th>
            <th>Tác vụ</th>
          </tr>
          </thead>
          <tbody>
          <% @regulations.each do |regulation| %>
            <tr class="gradeA">
              <td><%= regulation.title %></td>
              <td><%= regulation.body %></td>
              <td class="project-status">
                <% if regulation.notable? %>
                  <span class="label label-danger"><%= regulation.status_i18n %></span>
                <% else %>
                  <span class="label label-default"><%= regulation.status_i18n %></span>
                <% end %>
              </td>
              <td><%= regulation.created_at.strftime("%d-%m-%Y") %></td>
              <td>
                <%= link_to edit_admin_regulation_path(regulation), class:"btn btn-white btn-sm" do %>
                  <i class="fa fa-pencil"></i> Cập nhật
                <% end %>
                <%= link_to admin_regulation_path(regulation), method: :delete, data: { confirm: "Bạn có chắc chắc muốn xoá quy định này?" }, class:"btn btn-white btn-sm" do %>
                  <i class="fa fa-trash"></i> Xoá
                <% end %>
              </td>
            </tr>
          <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</div>